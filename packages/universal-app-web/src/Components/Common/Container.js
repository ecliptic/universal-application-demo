import styled from 'styled-components'
import {Constants} from 'universal-app-core'

const {glowGreen} = Constants.COLORS

export default styled.div`
  color: ${glowGreen};
  display: flex;
  flex-direction: column;
  align-items: center;
`
